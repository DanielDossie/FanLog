#include <iostream>
#include "BSTTemp.h"

using namespace std;

/*template <class T>
void BSTTemp<T>::insert(T object){
    if (root == nullptr) {
        root = object;
    }
    else {
        BSTNode curr = root;
        while (curr!=nullptr) {
            if (BSTNode->data < curr->data) {
                if (curr->left == nullptr){
                    curr->left = object;
                }
                else {
                    curr = curr->left;
                }
            }
            else {
                if (curr->right == nullptr){
                    curr->right = object;
                    curr = nullptr;
                }
                else {
                    curr = curr->right;
                }
            }
        }
    }
    cout << "insert called" << endl;
}

template <class T>
void BSTTemp<T>::show() {
    cout << "BST SHOW CALLED" << endl;
}*/